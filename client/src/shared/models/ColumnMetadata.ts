export interface ColumnMetadata {
  section: string;
  example: string;
  format: string;
  reason: string;
  definition: string;
  requirementLevel: string;
  requirementString?: string;
  formattedName: string;
  propertyName: string;
}
