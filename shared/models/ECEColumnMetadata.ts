export interface ECEColumnMetadata {
  section: string;
  example: string;
  format: string;
  reason: string;
  definition: string;
  required: string;
  formattedName: string;
  propertyName: string;
}
